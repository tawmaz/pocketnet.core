name: make check

on:
  workflow_dispatch:
  push:
    branches: [ feature/sqlite ]
  pull_request:
    branches: [ feature/sqlite ]

jobs:
  build:

    runs-on: ubuntu-20.04

    steps:
    - uses: actions/checkout@v2
    - name: ls
      run: ls -al
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install libdb-dev build-essential libtool autotools-dev automake pkg-config libssl-dev libevent-dev bsdmainutils python3 libboost-system-dev libboost-filesystem-dev libboost-chrono-dev libboost-test-dev libboost-thread-dev
    - name: autogen
      run: ./autogen.sh
    - name: configure
      run: ./configure --with-incompatible-bdb
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make deploy
      run: make deploy
